apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
spec:
  template:
    spec:
      containers:
      - name: test-container
        image: busybox
        command: ["sh", "-c", "echo Hello from smart scheduler! && sleep 10"]
      restartPolicy: Never
  backoffLimit: 2

